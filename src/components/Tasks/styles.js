import { StyleSheet } from 'react-native';
import { taskBackground, primaryColor, secondaryColor } from "./../../includes/variables"

const styles = StyleSheet.create({
    container: {
        flex: 1,
        alignSelf: 'stretch',
        backgroundColor: taskBackground
    }
})
export default styles;